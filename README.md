# DEMO_VERSION_CONTROL
DEMO_VERSION_CONTROL
 - 1. Add file คู่มือการใช้งาน 
 - 2. Add file คู่มือเวอร์ชัน -- commit 2

# 🧭 Git Commit Version Control Guide

คู่มือนี้สรุปขั้นตอนสำคัญเกี่ยวกับการโคลน (clone), ย้อนกลับไปยัง commit ที่ต้องการ (checkout commit) และการกลับไปยัง commit ล่าสุดหรือ branch อื่น ๆ

---

## 🔹 1. โคลน (Clone) Repository

โคลน repository จาก GitHub หรือ Git Server:

```bash
git clone <url>
```

ตัวอย่าง:
```bash
git clone https://github.com/user/project.git
cd project
```

---

## 🔹 2. Checkout ไปยัง Commit ที่ต้องการ

หากต้องการย้อนกลับไปดูเวอร์ชันของ commit ที่เฉพาะเจาะจง:

```bash
git checkout <commit-hash>
```

ตัวอย่าง:
```bash
git checkout a1b2c3d4
```

> 🔸 ตอนนี้คุณจะอยู่ในสถานะ **detached HEAD** (ไม่ได้อยู่บน branch ใด)

หากต้องการสร้าง branch ใหม่จาก commit นั้น:
```bash
git checkout -b my-feature
```

---

## 🔹 3. Clone แบบบางส่วน (Shallow Clone)

กรณีต้องการโคลนเฉพาะบาง commit เพื่อประหยัดเวลาและพื้นที่:

```bash
git clone --depth 1 <url>
```

หากต้องการ commit เก่า ๆ ที่อยู่นอก depth:
```bash
git fetch origin <commit-hash>
git checkout <commit-hash>
```

---

## 🔹 4. ดาวน์โหลดเฉพาะไฟล์ของ Commit (ไม่ใช้ Git)

หากคุณต้องการเพียง source code snapshot ของ commit นั้นโดยไม่ต้อง clone ทั้ง repo  
สามารถดาวน์โหลดได้จาก GitHub โดยตรง:

```
https://github.com/<user>/<repo>/archive/<commit-hash>.zip
```

ตัวอย่าง:
```
https://github.com/torvalds/linux/archive/a1b2c3d4.zip
```

---

## 🔹 5. กลับไปยัง Commit ล่าสุดหรือ Branch อื่น

หากต้องการกลับไปยัง commit ล่าสุดใน `main`:

```bash
git checkout main
```

หรือในกรณี branch หลักชื่อ `master`:

```bash
git checkout master
```

กลับไปยัง branch ก่อนหน้า (ทางลัด):

```bash
git switch -
```

---

## 🔹 6. ตรวจสอบสถานะและประวัติ Commit

ดู branch ทั้งหมดใน repo:

```bash
git branch
```

ดู commit ล่าสุด:

```bash
git log --oneline -n 5
```

---

## ⚠️ หมายเหตุ: Detached HEAD คืออะไร?

เมื่อคุณ `checkout` ไปยัง commit ที่ไม่อยู่ใน branch  
Git จะเข้าสู่สถานะที่เรียกว่า **Detached HEAD**  
ในสถานะนี้คุณสามารถดูและแก้ไขไฟล์ได้ แต่ถ้าคุณ `commit` เพิ่มโดยไม่สร้าง branch ใหม่  
commit เหล่านั้นจะ “ไม่ผูกกับ branch ใด” และอาจหายไปได้

✅ วิธีแก้ที่ปลอดภัย:
```bash
git checkout -b new-branch
```
เพื่อสร้าง branch ใหม่จาก commit นั้นก่อนเริ่มแก้ไข

---

## 🔹 สรุปคำสั่งสำคัญ

| การกระทำ | คำสั่ง |
|-----------|---------|
| โคลน repo | `git clone <url>` |
| Checkout commit | `git checkout <hash>` |
| สร้าง branch ใหม่จาก commit | `git checkout -b <branch>` |
| กลับไป main ล่าสุด | `git checkout main` |
| กลับไป branch ก่อนหน้า | `git switch -` |
| ดู branch ทั้งหมด | `git branch` |
| ดู commit ล่าสุด | `git log --oneline` |

---

🧰 **แนะนำ**  
- ใช้ `git status` บ่อย ๆ เพื่อดูสถานะปัจจุบัน  
- ใช้ `git log --oneline --graph --decorate` เพื่อดูภาพรวมประวัติ commit  
- สร้าง branch ใหม่ทุกครั้งก่อนแก้ไข commit เก่าเพื่อป้องกันข้อมูลหาย

---

**Author:** DevOps Quick Reference  
**Version:** 1.0  

